# Install

1. Clone the repository.
2. Run `npm install`.
3. Start the app with `npm run dev` or run a CLI diagnosis with `npm run diagnose -- examples/cli/quickstart-bundle.json`.
4. Save reports locally or to the server-backed API once auth and storage are configured.

The fastest first run is the built-in support benchmark preset in the web app.
