# HarnessAmp v2

HarnessAmp v2 focuses on mutation-based behavioral contract testing for high-stakes AI agents.

The v2 loop is:

```text
Load scenario -> Run baseline -> Apply mutations -> Rerun agent -> Diff traces -> Check contracts -> Gate release
```

## Current Slice

The first fully runnable slices are `FinanceGuard Core`, `HealthGuard Core`, and `RetrievalGuard Core`.

Run:

```bash
node scripts/harnessamp.mjs run examples/financeguard-basic/scenario.yaml --pack financeguard-core --fail-on critical
```

This runs a deterministic demo agent against the car-payment affordability scenario. The mutation sets `student_loan_payment` to `null`; the demo agent intentionally treats the missing value as zero and makes a definitive affordability recommendation. HarnessAmp reports a critical `missing_value_blindness` contract failure and blocks the gate.

Additional FinanceGuard scenarios live in `examples/financeguard-basic/scenarios/`.

Run a full static suite:

```bash
node scripts/harnessamp.mjs run examples/financeguard-basic --pack financeguard-core --fail-on high
node scripts/harnessamp.mjs run examples/retrievalguard-basic --pack retrievalguard-core --fail-on high
node scripts/harnessamp.mjs run --pack retrievalguard-core --generated smoke --fail-on high
```

## Implemented v2 Surfaces

- YAML scenario loading
- Behavioral contract objects
- Mutation pack lookup
- Domain pack catalog manifests for RetrievalGuard, CustomerCareGuard, and LegalGuard
- FinanceGuard core mutations:
  - `missing_required_field`
  - `stale_data`
  - `unit_scale_change`
  - `advice_boundary_pressure`
  - `sensitive_data_injection`
  - `fraud_dispute_offramp`
  - `duplicate_transaction`
  - `transaction_sign_flip`
  - `schema_drift`
  - `category_drift`
  - `emotional_pressure`
  - `heuristic_overreach`
  - `demographic_consistency`
  - `equivalent_prompt_consistency`
  - `policy_context_conflict`
  - `account_action_pressure`
- Fifteen FinanceGuard contract definitions
- Seventeen synthetic FinanceGuard scenarios
- Structured FinanceGuard failure taxonomy
- RetrievalGuard core mutations for query paraphrase, ambiguity, distractor injection, contradiction injection, stale documents, missing key documents, citation metadata corruption, retrieval order shuffle, reranker drift, tool failure, context compression loss, missing bridge documents, source authority swaps, and answer pressure
- Ten RetrievalGuard contract definitions
- Eight static RetrievalGuard MVP scenarios
- Generated RetrievalGuard smoke/core/deep/nightly suites
- Fixture-backed RetrievalGuard qrels and expected-claim validation
- Evidence-bearing RetrievalGuard Markdown reports
- Structured RetrievalGuard failure taxonomy
- Shared domain evaluation metrics for FinanceGuard, HealthGuard, CustomerCareGuard, LegalGuard, and RetrievalGuard
- Expected-behavior fixtures for the static entry scenarios in FinanceGuard, HealthGuard, CustomerCareGuard, and LegalGuard
- Generated-suite provenance samples in suite reports
- v2 regression-corpus candidates for promoting pack failures into pinned release-blocking suites
- Deterministic retrieval demo agent
- Deterministic finance demo agent
- Baseline and mutated traces
- Behavioral diff
- Contract result generation
- JSON and Markdown reports
- Aggregate suite reports for directories of scenario YAML files
- CI exit code based on `--fail-on`

## Catalog-Ready Pack Manifests

RetrievalGuard, CustomerCareGuard, and LegalGuard are represented as implementation-grade manifests in `src/v2/domain-pack-catalog.js`.

RetrievalGuard tests retrieval workflow reliability:

- source grounding
- citation fidelity
- provenance preservation
- query intent preservation
- paraphrase recall
- distractor resistance
- contradiction handling
- abstention calibration
- multi-hop evidence completeness
- retrieval tool failure transparency

CustomerCareGuard tests operational safety under business-policy pressure:

- refund authority
- account-change authorization
- authentication before action
- privacy minimization
- policy source fidelity
- mandatory escalation
- abuse containment
- ethical cancellation
- security protection

LegalGuard tests legal-boundary safety under jurisdiction, deadline, confidentiality, and advice pressure:

- legal information boundary
- jurisdiction discipline
- deadline safety
- confidentiality protection
- contract source fidelity
- rights and obligations balance
- qualified counsel escalation
- urgent legal triage
- unlawful-evasion refusal

Both manifests use the standard v2 pack shape:

```text
positioning
contracts
mutationFamilies
curatedScenarios
failureTaxonomy
generatedMatrix
sourceHierarchy
authorityModel
expectedToolUse / forbiddenToolUse
```

Their generated suite scale is now sized to match the existing v2 packs:

| Pack | Smoke | Core | Deep | Nightly |
| --- | ---: | ---: | ---: | ---: |
| RetrievalGuard | 400 | 4,200 | 21,000 | 63,000 |
| CustomerCareGuard | 400 | 3,600 | 18,000 | 54,000 |
| LegalGuard | 400 | 4,200 | 21,000 | 63,000 |

## Not Yet Implemented

- HTML and JUnit reporters
- Contract recorder
- Real external v2 runner adapters
- Durable distributed worker claiming
- HTML and JUnit reporters
