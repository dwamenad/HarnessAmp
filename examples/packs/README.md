# Benchmark pack examples

- Use `npm run compile:traces -- --pack` to generate a first-class benchmark pack from approved traces.
- Store reviewed packs here when you want a portable artifact that combines `intent`, `contract`, `benchmark`, and `wrapper`.
- The benchmark-pack schema lives at `docs/schemas/benchmark_pack.schema.json`.
