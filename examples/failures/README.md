# Failure examples

- Use `npm run collect:failures -- <bundle.json> <observations.json>` to generate new corpus entries.
- Store merged corpora privately when they reflect production regressions.
- Treat this directory as a public example surface, not the real moat.
