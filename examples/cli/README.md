# CLI Examples

Use these files when you want to exercise `npm run analyze` from the terminal.

- `quickstart-bundle.json` - a starter bundle for report generation
- `observed-runs.json` - sample observed outcomes for the same harness

