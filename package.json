{
  "name": "harnessamp",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test",
    "analyze": "node scripts/analyze.mjs"
  },
  "devDependencies": {
    "vite": "^6.1.0"
  }
}

